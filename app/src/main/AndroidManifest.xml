<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ooi.diloabsensi">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignUp"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".Berita" />
        <activity android:name=".DaftarHadir" />
        <activity android:name=".Absensi" />
        <activity
            android:name=".MenuTab"
            android:label=" "
            android:theme="@style/AppTheme" />
        <activity android:name=".IsiBerita" />
        <activity android:name=".InputAcara"
            android:theme="@style/AppTheme.Dark.InputAcara"/>
    </application>

</manifest>